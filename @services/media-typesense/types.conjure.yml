types:
  definitions:
    default-package: mediatypesense

    objects:
      Media:
        fields:
          id: string
          type: MediaType
          title: string
          description: string
          releaseDate: integer
      MediaType:
        values:
          - MOVIE
